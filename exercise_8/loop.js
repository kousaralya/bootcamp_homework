const people = ["Greg", "James","Matt", "Mary", "foo"];
let nLen = people.length;

for (let i = 0; i < nLen; i++) {
    i += people[i];
}
 console.log(people)

 const names = ["Greg", "James","Matt", "Mary", "foo"]
  
      let name = names.pop(0);
      console.log(names)